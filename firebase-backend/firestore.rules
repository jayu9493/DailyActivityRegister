/**
 * Firestore Security Rules for Daily Activity Register
 * 
 * CURRENT: Open access for development
 * TODO: Add authentication before production
 */

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Projects collection
    match /projects/{projectId} {
      // Allow all operations for now
      // TODO: Add authentication rules
      allow read: if true;
      allow write: if true;
      allow create: if true;
      allow update: if true;
      allow delete: if true;
    }
    
    // Future: Add authentication rules
    // Example with Firebase Auth:
    // allow read, write: if request.auth != null;
    
    // Future: Add role-based access
    // Example:
    // allow read: if request.auth != null;
    // allow write: if request.auth != null && 
    //                 request.auth.token.role == 'admin';
  }
}
